[project]
name = "twitcharr"
version = "1.3.2"
description = "Twitch EPG and stream integration plugin for Dispatcharr"
requires-python = ">=3.11"
dependencies = [
    "django>=4.2",
    "requests>=2.31",
]
